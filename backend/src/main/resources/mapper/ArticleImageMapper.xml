<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.gosleep.blog.mapper.ArticleImageMapper">

    <select id="queryByArticleId" resultType="top.gosleep.blog.bean.entity.FileStorage">
        SELECT
            fs.id,
            fs.mine_type as mineType,
            fs.name,
            fs.size,
            fs.created_at as createdAt
        FROM file_storage fs
            JOIN article_image ai ON ai.file_id = fs.id
        WHERE ai.article_id = #{articleId}
    </select>
</mapper>